const initialState = {
    loggedIn: false,
    username: ''
};

export const actionCreators = {
};

export const reducer = (state, action) => {
    state = state || initialState;
    return state;
};